{
  "name": "mann-center-prototype",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "check": "node --check dist/app.js && node --check dist/data.js && node --check server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
